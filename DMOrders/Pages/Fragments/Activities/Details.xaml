<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DMOrders.Pages.Fragments.Activities.Details"
             xmlns:customrows="clr-namespace:DMOrders.Controls.CustomRows.Lite"             
             xmlns:m="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
             Title="Actividades">
    <ContentPage.Resources>
        <ResourceDictionary>
            <OnPlatform x:Key="IsBackButtonEnabled" x:TypeArguments="x:Boolean">
                <On Platform="Android" Value="False" />
                <On Platform="iOS" Value="True" />
                <On Platform="WinUI" Value="True" />
            </OnPlatform>
        </ResourceDictionary>
    </ContentPage.Resources>
    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsEnabled="{StaticResource IsBackButtonEnabled}" IsVisible="{StaticResource IsBackButtonEnabled}"/>
    </Shell.BackButtonBehavior>

    <Grid RowDefinitions="Auto,Auto,*,Auto,Auto">
        <!-- Top title bar -->
        <Grid Grid.Row="0"
          BackgroundColor="WhiteSmoke"
          ColumnDefinitions="Auto,*"
          RowDefinitions="*,*"
          ColumnSpacing="20"
          VerticalOptions="Center">
            <Button Grid.Column="0" Grid.Row="0"
            Text=""
            Clicked="ButtonClose_Clicked"
            BackgroundColor="DarkGray"
            TextColor="White"
            HeightRequest="40"
            WidthRequest="40"
            Padding="0">
                <Button.ImageSource>
                    <FontImageSource 
                Size="25"
                FontFamily="FontAwesome5Solid"
                Glyph="&#xf060;" />
                </Button.ImageSource>
            </Button>

            <Label Grid.Column="1" Grid.Row="0"
                   x:Name="lblMainTitle"
               Text="Actividades Diarias No. {}   Fecha {}"
               HorizontalOptions="Fill"
               VerticalOptions="Center"
               HorizontalTextAlignment="Center"
               FontAttributes="Bold"
               FontSize="15" />
        </Grid>

        <Grid Grid.Row="1" BackgroundColor="LightGray" ColumnSpacing="4" Margin="0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.3*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Text="ID" FontAttributes="Bold" FontSize="11" HorizontalTextAlignment="Center" VerticalOptions="Center"/>            
            <Label Grid.Column="1" Text="EMPRESA" FontAttributes="Bold" FontSize="11" HorizontalTextAlignment="Start" VerticalOptions="Center"/>
            <Label Grid.Column="2" Text="MOTIVO" FontAttributes="Bold" FontSize="11" HorizontalTextAlignment="Start" VerticalOptions="Center"/>
            <Label Grid.Column="3" Text="OBS" FontAttributes="Bold" FontSize="11" HorizontalTextAlignment="Start" VerticalOptions="Center"/>
            <Label Grid.Column="4" Text="CLIENTE" FontAttributes="Bold" FontSize="11" HorizontalTextAlignment="Start" VerticalOptions="Center"/>
            <Label Grid.Column="5" Text="H. INI" FontAttributes="Bold" FontSize="11" HorizontalTextAlignment="Center" VerticalOptions="Center"/>
            <Label Grid.Column="6" Text="H. FIN" FontAttributes="Bold" FontSize="11" HorizontalTextAlignment="Center" VerticalOptions="Center"/>
            <Label Grid.Column="7" Text="STANDBY" FontAttributes="Bold" FontSize="11" HorizontalTextAlignment="Center" VerticalOptions="Center"/>
        </Grid>

        <CollectionView 
            Grid.Row="2"
                x:Name="MyCollectionView"
                ItemsSource="{Binding Activities}"
                ItemSizingStrategy="MeasureFirstItem"
                EmptyView="No hay actividades ingresadas..." 
                HorizontalOptions="Fill" 
                SelectionMode="Single"
                SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                Margin="0"
                BackgroundColor="Transparent">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <customrows:AccountAnalyticLineRow 
                        BindingContext="{Binding .}"     
                        EditCommand="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=EditCommand}"
                        DeleteCommand="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=DeleteCommand}"
                        Style="{StaticResource CollectionItemSelectable}"/>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Label Grid.Row="3"
               x:Name="lblSyncMessage"
               Margin="10,4"
               FontSize="12"
               TextColor="DarkOrange"
               LineBreakMode="WordWrap"
               IsVisible="False" />

        <HorizontalStackLayout Grid.Row="4" HorizontalOptions="Center" Margin="0,0,0,10" Spacing="10">
            <Button Text="" Clicked="ButtonClose_Clicked" BackgroundColor="Gray" TextColor="White" HeightRequest="43" WidthRequest="43" Padding="0" IsVisible="False">
                <Button.ImageSource>
                    <FontImageSource 
                        Size="25"
                        FontFamily="FontAwesome5Solid"
                        Glyph="&#xf060;"
                        />
                </Button.ImageSource>
            </Button>
            <Button x:Name="btnNuevo" Text="Nuevo" Clicked="ButtonNew_Clicked" BackgroundColor="Green" TextColor="White" HeightRequest="43" WidthRequest="120" Padding="0">
                <Button.ImageSource>
                    <FontImageSource 
                        Size="25"
                        FontFamily="FontAwesome5Solid"
                        Glyph="&#x2b;" />
                </Button.ImageSource>
            </Button>
            <Button Text="Guardar" Clicked="ButtonSave_Clicked" BackgroundColor="Orange" TextColor="White" HeightRequest="43" WidthRequest="120" Padding="0" IsVisible="False">
                <Button.ImageSource>
                    <FontImageSource 
                        Size="25"
                        FontFamily="FontAwesome5Solid"
                        Glyph="&#xf0c7;" />
                </Button.ImageSource>
            </Button>
            <Button x:Name="btnEnviar" Text="Enviar" Clicked="ButtonSync_Clicked" BackgroundColor="DeepSkyBlue" TextColor="White" HeightRequest="43" WidthRequest="120" Padding="0">
                <Button.ImageSource>
                    <FontImageSource 
                        Size="20"
                        FontFamily="FontAwesome5Solid"
                        Glyph="&#xf1d8;" />
                </Button.ImageSource>
            </Button>
            <Button x:Name="btnReprocesar"
                    Text="Reprocesar"
                    Clicked="ButtonReprocess_Clicked"
                    BackgroundColor="DarkOrange"
                    TextColor="White"
                    HeightRequest="43"
                    WidthRequest="140"
                    Padding="0"
                    IsVisible="False">
                <Button.ImageSource>
                    <FontImageSource
                        Size="18"
                        FontFamily="FontAwesome5Solid"
                        Glyph="&#xf2f1;" />
                </Button.ImageSource>
            </Button>
        </HorizontalStackLayout>
    </Grid>
</ContentPage>
